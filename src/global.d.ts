// Build-time constants injected by tsup define.
// When running via tsx (dev), these fall back to sensible defaults in version.ts.
declare const __HEKU_VERSION__: string;
declare const __HEKU_NAME__: string;
